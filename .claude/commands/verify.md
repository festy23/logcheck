Run the full verification suite for logcheck.

Delegate to the **verifier** agent: "Run the full verification loop on the current state of the codebase."

If verifier reports failures that it cannot fix, surface them clearly to the user.
